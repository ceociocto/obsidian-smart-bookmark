import { ChromeParser } from "./chrome";

/**
 * Edge bookmarks parser
 * Edge uses the same JSON format as Chrome
 */
export class EdgeParser extends ChromeParser {
	// Edge bookmarks have the same structure as Chrome
	// So we just extend ChromeParser
}
